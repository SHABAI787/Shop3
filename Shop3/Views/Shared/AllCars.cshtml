@model Shop3.Data.Models.Car

<div class="col-lg-4">
    <img class="img-thumbnail" src="@Model.img" alt="@Model.name" />
    <h2>Модель: @Model.name</h2>
    <p>@Model.shortDesc</p>
    <p>Цена: @Model.price.ToString("c")</p>
    <p><a class="btn btn-warning" href="#">Подробнее</a></p>
    <!-- Добавил свою кнопку для проверки -->
    <p><a class="btn btn-warning" href="https:\\www.yandex.ru">Купить</a></p>
</div>